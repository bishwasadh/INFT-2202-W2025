<%- include('header.ejs') -%>
    <h1>My Pokemon Party</h1>
    <% if (pokemonParty.length > 0) { %>
        <% pokemonParty.forEach(function(pokemon) { %>
            <p>Name: <%= pokemon.name %></p>
            <p>weight: <%= pokemon.weight %></p>
            <img src="<%= pokemon.image %>" style="height:300px; width: auto;">
            <a href="/editPokemon/<%= pokemon._id %>"><button>Edit Pokemon</button></a>
            <form action="/deletePokemon" method="POST">
                <input hidden type="text" name="pokemonId" value="<%= pokemon._id %>" />
                <button type="submit">Delete</button>
            </form>
        <% }) %>
    <% } else { %>
        <p>You have no pokemon in your party :( Please save a pokemon in your party to see them here</p>
    <% } %>
    <%- include('footer.ejs') -%>